# 强制使用PNPM包管理器
engine-strict=true

# 优化依赖解析
prefer-offline=true

# 解决Cloudflare Pages中的兼容性问题
node-linker=isolated

# 避免不必要的可选依赖
override-optional=true

# 为Cloudflare Pages环境配置
save-exact=true

# 修复可能的依赖解析问题
resolution-mode=highest

# 避免使用Node.js特定的依赖
shell-emulator=false